Added: logins.createdAt

New login rows, including datasets created from test taker templates, receive
their creation date and time automatically.

For existing template datasets with recorded activity, createdAt is backfilled
from the earliest available first-login timestamp. Existing datasets without
recorded activity remain NULL because their creation time cannot be recovered
from the previous database structure.

VERSION: 052
